{
  "name": "@dnb-hugo/MODULENAME",
  "version": "0.0.0",
  "description": "DESCRIPTION",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "homepage": "https://github.com/dnb-hugo/components/tree/master/MODULENAME",
  "license": "MIT",
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"MODULENAME/v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"MODULENAME/v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"MODULENAME/v\" && git push --follow-tags origin main"
  },
  "bugs": {
    "url": "https://github.com/dnb-hugo/components/issues"
  },
  "devDependencies": {
    "standard-version": "9.0.0"
  },
  "standard-version": {
    "skip": {
      "changelog": true
    },
    "bumpFiles": [
      {
        "filename": "package.json",
        "type": "json"
      }
    ]
  },
  "private": true
}
