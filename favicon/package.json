{
  "name": "@dnb-hugo/favicon",
  "version": "1.0.7",
  "description": "Favicons for Hugo",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "homepage": "https://github.com/dnb-hugo/components/tree/master/favicon",
  "license": "MIT",
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"favicon/v\" --releaseCommitMessageFormat \"chore(release): favicon/v{{currentTag}}\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"favicon/v\" --releaseCommitMessageFormat \"chore(release): favicon/v{{currentTag}}\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"favicon/v\" --releaseCommitMessageFormat \"chore(release): favicon/v{{currentTag}}\" && git push --follow-tags origin main"
  },
  "bugs": {
    "url": "https://github.com/dnb-hugo/components/issues"
  },
  "devDependencies": {
    "standard-version": "9.0.0",
    "@commitlint/cli": "11.0.0",
    "@commitlint/config-conventional": "11.0.0",
    "husky": "4.3.6",
    "@dnb-hugo/commitlint-config-dnb": "github:dnb-hugo/commitlint-config-dnb"
  },
  "standard-version": {
    "skip": {
      "changelog": true
    },
    "bumpFiles": [
      {
        "filename": "package.json",
        "type": "json"
      }
    ]
  },
  "private": true,
  "commitlint": {
    "extends": [
      "@dnb-hugo/commitlint-config-dnb"
    ]
  }
}
