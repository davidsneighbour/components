{
  "name": "@dnb-hugo/robots",
  "version": "1.0.1",
  "description": "Configurable robots.txt for Hugo",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "homepage": "https://github.com/davidsneighbour/dnb-hugo/tree/master/robots",
  "license": "MIT",
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"robots/v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"robots/v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"robots/v\" && git push --follow-tags origin main"
  },
  "bugs": {
    "url": "https://github.com/davidsneighbour/dnb-hugo/issues"
  },
  "devDependencies": {
    "standard-version": "9.0.0"
  },
  "standard-version": {
    "skip": {
      "changelog": true
    },
    "bumpFiles": [
      {
        "filename": "package.json",
        "type": "json"
      }
    ]
  },
  "private": true
}
