{
  "name": "@dnb-hugo/sitemap",
  "version": "0.0.0",
  "description": "A sitemap for Hugo",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "homepage": "https://github.com/davidsneighbour/dnb-hugo/tree/master/sitemap",
  "license": "MIT",
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"sitemap/v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"sitemap/v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"sitemap/v\" && git push --follow-tags origin main"
  },
  "bugs": {
    "url": "https://github.com/davidsneighbour/dnb-hugo/issues"
  },
  "devDependencies": {
    "standard-version": "9.0.0"
  },
  "standard-version": {
    "skip": {
      "changelog": true
    },
    "bumpFiles": [
      {
        "filename": "package.json",
        "type": "json"
      }
    ]
  },
  "private": true
}
