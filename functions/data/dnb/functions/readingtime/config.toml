# this should be a float value, not integer!
wordsperminute = 220.0
# should the function return minutes and seconds or only minutes (the latter
# will be rounded up for the seconds in addition).
minutesandseconds = true
#   # increasing the reading time by frontmatter `readingtimeincrease=x` setup.
#   # either via minutes:seconds or minutes.(seconds*0.6).
#   frontmatterincrease = false
